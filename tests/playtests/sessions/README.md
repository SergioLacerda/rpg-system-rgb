# Core V2 Playtest Sessions

This directory stores human playtest session records.

Playtest records are experience evidence, not automated validation. Use the
templates in `../templates/` to capture what players understood, which choices
they considered, and whether the encounter state changed in meaningful ways.

Automated rule behavior belongs in Go tests and feature files. Human playtests
belong here.
