{
  "name": "rgb-system-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=26 <27",
    "npm": ">=11 <12"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "lint": "astro check",
    "format": "prettier --write \"src/**/*.{astro,ts}\" \"*.{mjs,ts,json}\"",
    "lint:fix": "npm run format && astro check",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@vitest/coverage-v8": "^4.1.11",
    "prettier": "^3.6.2",
    "prettier-plugin-astro": "^1.0.0",
    "typescript": "^6.0.3",
    "vitest": "^5.0.0"
  }
}
