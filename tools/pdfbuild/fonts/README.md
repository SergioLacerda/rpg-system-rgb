# PDF Authoring Fonts

Optional local font files for the manual PDF authoring step belong here.

Do not reference fonts from a CDN. If custom fonts are added, update
`docs/styles/rgb-pdf.css` with relative `@font-face` URLs and record the font
license in this directory.
